{
    "name": "@gtkx/e2e",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "tsc -b",
        "test": "vitest run",
        "coverage": "vitest run --coverage && sed -i 's|^SF:\\.\\./|SF:packages/|' coverage/lcov.info",
        "typecheck": "tsc -b --emitDeclarationOnly"
    },
    "dependencies": {
        "@gtkx/ffi": "workspace:*",
        "@gtkx/react": "workspace:*",
        "@gtkx/testing": "workspace:*"
    },
    "devDependencies": {
        "@gtkx/vitest": "workspace:*",
        "react": "^19.2.5"
    }
}
